<form method="post" action="{{ path('app_recette_delete', {'id': dbara_recette.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dbara_recette.id) }}">
	<button class="btn btn-danger">Supprimer</button>
</form>
